http_service_regexes:
  - "([a-z]+.)?wikipedia.org"
  - "([a-z]+.)?wikimedia.org"
  - "([a-z]+.)?mediawiki.org"
  - "([a-z]+.)?wikidata.org"
  - "([a-z]+.)?wikisource.org"

# TODO: resolve these IPs on startup
ips_to_listen_to:
  - 208.80.154.224 # meta.wikimedia.org
  - 185.15.58.224 # dyna.wikimedia.org -> *.wikipedia.org, commons.wikimedia.org, ...

interesting_users_prefix: "tools."
